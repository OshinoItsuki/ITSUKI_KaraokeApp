# ITSUKI

**Interactive Terminal Service for Unified Karaoke Integration**

ITSUKIは、PCに保存したカラオケ動画をスマホやPCのブラウザから検索・予約し、VLCで順番に再生するローカルカラオケ統合ツールです。

## できること

- 曲名・人物・タイアップ・年代・外国語曲などから選曲
- 新曲・更新曲を週（W番号）単位で確認
- 人物検索の完全一致 / 部分一致 / 前方一致、別名義・グループ候補の表示切替
- 人物ごとに歌手 / 作詞 / 作曲 / 編曲の担当曲を横断表示
- スマホやPCから予約、予約順変更、リモコン操作
- 選曲者に加えて「一緒に歌う人」を参加者から登録
- 「＋♪ とりあえず300」に気になる曲を一旦保存
- DBが無い動画も「フォルダから選曲」から直接予約
- VLCによる動画再生、曲終了時のNEXT表示・目安Kcal演出
- 履歴・ランキング（総合 / 年別 / ジャンル別）と過去Excelデータの取込・出力
- 歌手別TOP3・再生ゼロ曲タグ、再生ゼロ曲からのおすすめ
- ON/OFFボーカル判定
- 動画↔楽曲DBの紐づけ監査 / 監査Excelでの一括修正・確認状態・通報ステータス管理
- 動画ルート直下の `.itsuki` にルート設定・DB別紐づけを保存し、SSD/HDDの持ち寄り・クローン・別PC運用で自動引継ぎ
- 設定・履歴・紐づけ情報などの選択バックアップ / 復元
- Googleスプレッドシートへの予約リスト同期（任意）

## 必要なもの

- Windows PC
- [VLC media player](https://www.videolan.org/vlc/)
- [Google Chrome](https://www.google.com/chrome/)
- 自分で用意したカラオケ動画
- 楽曲DBは任意

## はじめかた

1. [Releases](https://github.com/OshinoItsuki/ITSUKI_KaraokeApp/releases) から最新版ZIPをダウンロードして展開します。
2. VLCとChromeが無ければインストールします。
3. `ITSUKI.exe` を起動します。
4. ITSUKI本体PCでは、リモコン画面で `5963` →「メモリーセット」と操作するとPINなしで管理画面を開けます。スマホなど未登録端末は、続けて管理者PIN（初期値 `0000`）を入力します。
5. 動画ルートを設定して「動画を再スキャン」を実行します。
6. スマホを同じLAN / Wi-Fiへ接続してTOP画面から選曲します。

## 楽曲DBがなくても使えます

楽曲DBは必須ではありません。DBが無い場合でも、スキャン済みの動画をフォルダ階層から探して予約できます。詳しくは [フォルダから選曲](https://oshinoitsuki.github.io/ITSUKI_KaraokeApp/#folder-selection) を確認してください。

楽曲DBを自分で作りたい場合は、[KaraokeSong DB Editorはこちらからダウンロードできます](https://github.com/OshinoItsuki/KaraokeSong_DB_Editor)。

## 詳しい使い方

管理者向けの詳しい設定・操作手順は **[Web版HELP](https://oshinoitsuki.github.io/ITSUKI_KaraokeApp/)** で確認できます。Release版には同じ内容の `help.html` も同梱しているため、ITSUKIを使うPCではインターネット接続なしでも確認できます。

## 更新・開発状況

ITSUKIの更新情報や開発中の内容は、X（Twitter）の **#ITSUKI_KaraokeApp** でも投稿しています。  
[更新・開発状況をXで見る](https://x.com/search?q=ITSUKI_KaraokeApp)

## バグ報告・機能追加要望

[GitHub Issues](https://github.com/OshinoItsuki/ITSUKI_KaraokeApp/issues) からお願いします。

## 注意

ITSUKIは動画・音源・楽曲DBそのものを配布しません。利用する動画・音源・データの権利や利用条件は各自で確認してください。



## v1.1.4
- タイアップ一覧のカテゴリ細分類を、別画面へ進む階層ナビゲーションから一覧上部のフィルタボタンへ変更
- カテゴリを開いた時点でタイアップ一覧を直接表示し、TVアニメ / OVA / アニメ映画など利用可能な細分類だけをその場で絞り込み可能
- 五十音ボタンをスクロールジャンプから単一選択フィルタへ変更。同じ行を再選択すると解除、別の行を選ぶと前の選択を自動解除
- カテゴリ見出し・細分類・五十音をsticky領域へまとめ、長い一覧のスクロール中も上部に表示
- タイアップから曲一覧へ進んで戻った場合も、細分類・五十音の選択状態を保持

## v1.1.3
- 検索・履歴・カテゴリ一覧などの曲一覧へ、アニメ映像 / 特撮映像 / ライブ映像 / パート分けの特徴タグを固定幅で表示
- 4種類のどれにも該当しない曲は音符マークを表示し、複数該当時は複数タグを表示
- タイアップ表示を「カテゴリ細分類（無ければカテゴリ）『タイアップ名』OP/ED/IN」形式へ統一し、DBの「-」値は非表示
- 検索画面に4種類の特徴フィルタを追加。複数選択はAND条件、4種類すべてをONにした場合は全解除
- 履歴 / カテゴリ / 年代 / 新曲・更新曲 / 再生ゼロ曲 / とりあえず300にも共通の曲一覧表示を適用。ランキング / フォルダ選曲 / 予約曲リストは従来表示を維持

## v1.1.2
- 管理画面の「スタート」で操作用 `/queue`（予約曲リスト）を自動起動していた挙動を廃止
- カラオケ表示は専用Chrome `/display` とVLCだけで管理し、予約なし時は黒表示＋待機VLC、予約受信時は表示用予約一覧 → 曲VLC再生の順へ統一
- 操作用ページは「予約曲リスト」、曲再生前のプレゼンテーション表示は「予約一覧」と名称を整理
- 待機VLCの起動中に黒い表示Chromeを非アクティブTOPMOSTで維持し、VLCウインドウが一瞬前面へ出て「開く→消える→また出る」ように見える現象を抑制
- 待機VLCの準備完了・表示開始ログへPIDを追加し、同一プロセスのまま画面切替しているか確認可能に変更

## v1.1.1
- 選曲予約画面に「歌詞検索」と「Web検索」を追加
- 歌詞検索は楽曲DBの曲名・歌手名を使ってUtaTenの検索結果を新しいタブで表示
- Web検索は「曲名 歌手名 歌詞」の検索結果を新しいタブで表示
- 歌詞本文・歌い出しはITSUKI側へ保存しないため、ローカルDBやSSDの保存データ量は増加しない
- 曲名検索・人物検索・履歴・フォルダ選曲・とりあえず300等、共通予約画面を使うすべての入口で利用可能

## v1.1.0
- 動画ファイル命名規則を大幅拡張し、曲名だけでなく楽曲DBの主要項目を幅広く利用可能に変更
- `[song_id]` を追加。ファイル名にDBの曲IDを含めている場合は生の曲IDへ完全一致で照合し、曲名・歌手名に依存せず確実に候補を特定
- `[song]` を命名規則の必須項目から外し、曲ID・人物・タイアップ等のDB項目だけでも規則を構成可能
- 曲名ルビ・曲キーワード・別名・発売日/発売年、歌手/作詞/作曲/編曲の名前・ID・キーワード、タグ名/ID/キーワードを追加
- タイアップID/名前/ルビ/キーワード、カテゴリ/詳細カテゴリの名前・ID、シリーズ、OP/ED区分、作品発売日/発売年、DBファイル名にも対応
- 発売日はDB内部がMJDでも、ファイル名側の `YYYYMMDD` / `YYYY-MM-DD` / `YYYY.MM.DD` / `YYYY_MM_DD` を照合可能
- `[raw_song_id]` や `[tie_up_id]` などDB列名に近い互換タグも受付
- 管理画面とHELPへ使用できる命名規則タグ一覧と使用例を追加

## v1.0.50
- 管理画面の動画設定を「動画ルート別設定」へ変更し、SSD / HDDごとに独立した設定カードを表示
- 「除外フォルダ・ファイル」と「ファイル名から ON / OFF ボーカル判定」を同じ動画ルートのカード内で編集できるよう変更
- 除外設定は管理画面ではルート相対パスで表示し、保存時に既存設定へ安全に変換して `.itsuki/root_config.json` へルート別に保存
- 除外するフォルダ / ファイルの📁選択は、選択中の動画ルート配下だけを受け付けるようにして別ルートへの誤登録を防止
- 旧版の全ルート共通ON/OFFボーカル判定は、初回表示時に各動画ルートへ同じ判定語を展開して移行
- 単一DB / 複数DBどちらでも、現在設定されている動画ルートに合わせてルート別設定カードを表示
- 動画ルートの追加・変更・削除時も、未保存のルート別設定を可能な範囲で保持して画面を更新

## v1.0.49
- 各動画ルート直下に隠しフォルダ `.itsuki` を作成し、動画ライブラリと一緒に持ち運べる可搬設定を保存
- `.itsuki/manifest.json` にライブラリIDを保存。SSD/HDDを丸ごとクローンして別PCで使う場合も、同じライブラリ系統として設定を維持
- `.itsuki/root_config.json` に「除外フォルダ・ファイル」と「ファイル名からON/OFFボーカル判定」を保存。除外パスはルート相対で保持するため、D:→F:などドライブ文字が変わっても復元
- `.itsuki/linkage/DB別.json` に動画チェックサム・相対パス・曲ID・手動/自動紐づけ・確認状態を保存
- 別PCで動画ルートと楽曲DBを設定して再スキャンすると、フルパスに依存せず相対パスまたはv1.0.47の動画内容フィンガープリントから同一動画を特定して紐づけを復元
- DBファイルが完全一致する場合はSHA-256で対応する紐づけデータを選択。DB更新でハッシュが変わった場合も、同名DBまたは単一候補からraw曲IDを現在のDBへ再解決
- 「確認済み同士で異なる曲ID」「手動確定同士で異なる曲ID」は自動上書きせず「要確認」にして競合表示。競合解消前はSSD側の紐づけJSONを上書きしない
- 管理画面の手動紐づけ・解除・監査状態変更・監査Excel取込後は、該当ルートの可搬紐づけへ自動同期
- 家PCで紐づけを整備してイベント用ノートPCへSSDを移す運用、各自のSSD持ち寄り、同一SSDのクローンを別PCへ配布する運用に対応

## v1.0.48
- 人物ページの「別名義・所属」一覧を強化し、DBの人物関係マスタに登録があれば、その人物に現在選曲可能な曲が0件でも表示
- 関連人物の表示条件を「曲がある人物」から「kvdb_person_relation に直接関係が登録されている人物」へ変更
- 曲が0件の別名義・所属グループ・所属メンバーは「登録曲なし」と表示し、ボタンを無効化
- 曲が存在する関連人物は従来どおり曲数を表示し、人物ページへ移動可能
- 人物関係はDBごとに保持するため、複数DBモードでも関係のない別DBの人物関係を混ぜない

## v1.0.47
- 新曲・更新曲の判定日を「ファイル更新日時（mtime）」だけへ変更。SSDへのコピー日・ファイル作成日時・ITSUKI登録日時では新譜扱いにしない
- 動画内容の安定識別用に、ファイルサイズ＋複数位置サンプルをSHA-256化した内容フィンガープリントを動画DBと紐づけ記憶へ保存
- フルパスが変わっても内容フィンガープリントが一致すれば同一動画として曲ID・紐づけ状態・確認状態を継承
- 従来の「サイズ＋更新日時」一致は旧データ互換用の補助判定として維持
- 同じフィンガープリントに異なる曲IDの記録が競合する場合は、安全のため自動継承しない
- 監査Excel形式をv5へ更新し、非表示列に「動画チェックサム」を追加。インポートはフルパス → チェックサム → 相対パスの順で動画を特定
- v1～v4の旧監査Excelも引き続きインポート可能。v1.0.47更新後に一度動画再スキャンすると既存動画にもチェックサムが作成される

## v1.0.46
- TOPの「アニメ・ゲーム映像」の隣に「特撮映像」ボタンを追加
- 特撮映像はアニメ映像と同じく、判定タグに一致する曲をタイアップ作品ごとにまとめ、作品を開くと収録曲一覧を表示
- 特撮映像の標準判定タグとして「特撮映像」を追加し、管理画面から任意のタグへ変更・追加可能
- TOP中サイズボタンの表示設定にも「特撮映像」を追加。既存設定では自動的に表示ONで追加

## v1.0.45
- リモコンのテンポ±5%が表示上だけ変わり、VLC実再生速度へ反映されない環境がある問題を修正
- テンポ制御をVLC RCへの投げっぱなし方式から、ITSUKI同梱のlocalhost Lua制御へ変更し、実際のrate変更成功応答を確認してから現在値を更新
- VLC 3 / VLC 4のrate API差をLua側で吸収し、50%～150%の5%刻みを維持
- スタート時の待機画面で、表示Chromeの黒画面が実際に準備できる前にVLCを先行再生してしまう競合を修正
- 待機VLCは黒画面Chromeの実ウインドウ確認後だけ起動し、0秒への巻き戻し完了を確認してから1回だけ前面表示するよう変更

## v1.0.44
- 監査Excelで並び替えを行った際、表示列A～Oだけが移動して非表示の内部列P～Uが元の順番に残ることで、別動画の出力時曲ID・確認状態を誤読する問題を修正
- 新しく出力する監査Excelはオートフィルター範囲をA～Uへ拡張し、非表示列も必ず同じ行として並び替わるよう変更
- 既存の壊れた監査Excelは形式バージョンに関係なく自動検出し、動画フルパス＋表示列を正として読み込み、ずれた非表示列を無視して復旧
- この復旧モードでも「修正後の曲ID」の明示入力は最優先し、手動確定済みの現在側リンク保護は維持

## v1.0.43
- MV・PV / LIVEカラオケ / アニメ・ゲーム映像 / パート分けの分類ルールを、ITSUKI固定条件からユーザー設定可能な楽曲タグ方式へ変更
- 管理画面に「検索カテゴリのタグ判定」を追加し、各カテゴリへ複数タグをOR条件で指定可能
- 1曲が複数カテゴリへ同時所属可能。例：「アニメ映像」＋「MV」タグならアニメ映像とMV・PVの両方に表示
- アニメ映像の従来ルール「MADタグ＋アニメ/ゲーム系タイアップ」を廃止。MADは必要なカテゴリへ任意で追加する方式に変更
- パート分けの従来キーワード判定を廃止し、楽曲タグ判定へ統一
- 監査Excelの進捗表で、総件数行の割合100%は冗長なため非表示化

## v1.0.42
- KVDB v2の階層型「カテゴリ細分類」に対応。タイアップに細分類が登録されている場合は、大分類の下に細分類を表示
- カテゴリ細分類は上位・下位の階層をたどって絞り込みでき、細分類未登録のタイアップは「細分類なし」から参照可能
- タイアップ一覧・曲情報では、細分類がある場合に「大分類 > 細分類」の情報を優先表示
- 監査Excelの「使い方」シート上部へ、総件数・確認済み・自動判定・要確認の件数と割合をまとめた進捗表を追加
- 進捗表は全監査シートをExcel関数で合算。確認状態や修正後の曲IDを編集すると自動再計算
- 「自動判定」は未確認の自動紐づけかつ修正後の曲IDが未入力の行を集計

## v1.0.41
- 同じフォルダに複数の動画ファイル命名規則を登録できるよう変更
- 命名規則は表示順に上から試し、曲IDを一意に確定できた時点で終了。規則不一致・候補なし・同名候補で未確定の場合だけ次の規則へ進む
- 最も深いフォルダの設定を優先する従来仕様は維持し、そのフォルダ内の複数ルールだけを順番に評価
- 管理画面の複数行テキスト入力を廃止し、フォルダ単位のカード＋1ルール1行の入力欄へ変更。↑ / ↓で優先順位変更、追加・削除が可能
- 旧設定の「フォルダ → 1つの文字列」形式は自動的に1件のルールとして読み込み、互換性を維持

## v1.0.40
- 監査Excel取込で、出力後に内部DB側の確認状態・確認メモが変わっているだけで「再出力してください」エラーになる問題を修正
- 「確認状態」「確認メモ」は監査Excel側を取込時の最終値として反映し、古い監査Excelでもその2項目の差だけでは取込を止めないよう変更
- 同じ監査Excelを複数回取り込んでも、すでに反映済みの確認状態・確認メモを競合と誤判定しないよう修正
- 曲ID側の安全確認は維持し、確認済みだった曲IDが現在と異なる場合は「修正後の曲ID」への明示入力を引き続き要求

## v1.0.39
- 監査Excelを「動画パス ↔ 曲ID」のスナップショットとして取り込むよう変更
- 再スキャンで未確認の自動紐づけが別の曲IDへ変わっていても、監査Excel出力時の曲IDへ自動復元
- 動画の特定はフルパス完全一致を優先し、フルパスが見つからない場合は同じDBセット内の相対パス完全一致が1件だけなら復元対象として採用
- 「修正後の曲ID」が入力されている場合は、そのIDをスナップショットより優先して手動確定
- Excel出力時点ですでに「確認済み」だった行の曲IDが現在と異なる場合は自動上書きせず、正しいIDを「修正後の曲ID」へ明示して再確認
- Excel出力後に現在側で手動確定または確認済みへ変更された内容は、古いExcelから上書きしない

## v1.0.38
- 監査Excelで「修正後の曲ID」を手動入力した行が、未確認の自動紐づけ更新を「出力後変更」と誤判定して取り込めない問題を修正
- 修正後の曲IDを明示した行は、再スキャンで未確認の自動判定が変わっていてもExcel側の手動指定を優先して手動確定
- Web側ですでに手動確定または確認済みへ変更された紐づけは、従来どおり古いExcelから上書きしない
- 修正後IDを指定せず確認状態だけ編集した行は、出力後に紐づき先が変わっていた場合に確認済みにしないよう競合判定を強化

## v1.0.37
- 店舗DAM / JOYSOUND等のHDMI映像をキャプチャーボード経由でITSUKI PCへ入力し、通常リモコンから「ITSUKI / 店舗カラオケ」を切り替えるための映像入力機能を追加
- キャプチャー映像はVLC DirectShowで表示し、キャプチャー側の音声はITSUKIでは常時無効
- 通常はITSUKIを優先。ITSUKIの予約案内・演奏が始まると、店舗カラオケ映像を表示中でも自動的にITSUKI画面へ戻る
- 管理画面で映像キャプチャー機能のON/OFFとDirectShow映像デバイス名を設定可能。空欄時はVLC既定デバイスを使用
- 実キャプチャーボードごとのデバイス名・映像形式・HDCP等は実機確認で最終調整予定

## v1.0.36
- リモコンのテンポ操作を「−5% / ＋5%」の2ボタンへ簡素化
- 100%リセット専用ボタンを削除。次の曲では従来どおり自動で100%へ戻る

## v1.0.35
- リモコンにテンポ変更機能を追加し、再生中のVLC速度を5%刻みで変更可能
- テンポ範囲は50%～150%、5%刻み
- テンポ変更時はVLCのtime-stretchを使用し、再生速度変更で音程が一緒に動きにくい方式に変更
- 現在テンポをリモコン上に表示し、変更時は再生画面上部にも「テンポ 95%」等を短く表示
- 「はじめから」では現在テンポを維持し、次の曲へ進むと自動的に100%へ戻る
- キー変更機能とテンポ変更を同時利用可能

## v1.0.34
- フォルダごとに動画ファイルの命名規則を登録し、自動曲判定へ利用できるよう変更
- 命名規則タグとして `[song]`（曲名・必須）、`[artist]`、`[tieup]`、`[tag]`、`[vocal]`、`[ignore]`、`[number]` に対応
- 例：`[song]_[artist]_[vocal]` の場合、`Ai off` 等のボーカル情報は曲名判定から完全に除外し、`[artist]` は同名曲の絞り込みへ利用
- `[tieup]` や `[tag]` を含めると、同名曲・LIVE/MV等の候補判定にも利用
- 複数の命名規則が対象になる場合は最も深いフォルダの規則を優先。規則がないフォルダは従来のファイル名判定を使用
- 命名規則を設定したフォルダで規則に合わないファイルは、誤った自動紐づけを避けるため自動確定しない
- 手動確定済み、および自動判定後に「確認済み」にした紐づけは固定。未確認の自動判定は再スキャン時に新しい命名規則・判定ロジックで再評価し、より適切な曲が見つかれば差し替え

## v1.0.33
- 管理者PINで一度認証して管理画面へ入ったスマホ・別PCを、その画面から「このブラウザを管理端末として登録」できるよう変更
- 登録したブラウザは次回から管理者PINを省略でき、数字なしの「メモリーセット」10連打ショートカットも利用可能
- 管理画面に現在のブラウザが「本体PC / 登録済み / PIN認証済み・未登録」のどれかを表示し、登録済み一覧では現在操作中の端末へ「このブラウザ」と表示
- 従来のマスターURL / LAN用QR / Tailscale用QRによる端末登録もそのまま利用可能
- 現在操作中の信頼済み端末を個別解除・全解除した場合は、そのブラウザの信頼Cookieも同時に失効

## v1.0.32
- 曲紐づけが「確認済み」ではない動画は、予約一覧・予約プレビュー・予約通知・NEXT・履歴・ランキングで曲名/歌手名を信用せず、動画ファイル名を主表示するよう変更
- 未紐づけ / 未確認 / 要確認は状態を区別して補足表示し、誤った曲情報が予約一覧等へそのまま出る混乱を防止
- 履歴・予約一覧は予約当時の曲IDではなく、動画フルパスから現在の動画DBを参照して表示判定。後から正しく紐づけて「確認済み」にすると過去履歴も現在の曲名/歌手名表示へ自動更新
- NEXT表示も予約時フルパスの現在紐づけを優先して判定
- ランキングは動画パスがある履歴について、未確認中は動画フルパス単位で集計し、確認済みになった後は現在の曲ID単位へ自動的に再集計
- 予約送信/詳細画面は従来どおり現在の紐づき先曲情報を表示し、未確認/要確認の場合は警告を表示。間違い通報も引き続き利用可能

## v1.0.31
- マスターURLで登録した信頼済み管理端末でも、数字を入力せず「メモリーセット」を4秒以内に10回押すと管理画面へ直接移動できるよう変更
- 10連打ショートカットはITSUKI本体PCまたは信頼済み管理端末だけで有効。未登録端末では発動しない
- 管理画面のマスターURL欄を改善し、LAN用URLとTailscale用URLそれぞれに専用QRコードを表示
- 各QRコードに用途ラベルを付け、どの接続方法用か判別しやすく変更
- 従来の5963→メモリーセット経路も引き続き利用可能

## v1.0.30
- 管理画面の「スタート」時に表示用Chromeを先行起動していた処理を廃止し、予約プレビュー / 待機 / 黒画面の実際の遷移処理だけが表示Chromeを1回起動するよう修正
- Chrome専用プロファイル起動時の親プロセス切替を待つ猶予を延長し、初回ウインドウ検出が遅いPCで表示Chromeが二重起動する問題を防止
- 内部表示専用Chromeをタスクバー / Alt+Tabに通常のChromeとして出さないToolWindow扱いへ変更。操作用には予約一覧Chromeだけが見える構成へ整理
- 待機VLCはプロセス起動時点から無音にし、VLCウインドウ生成中は黒画面を前面固定。先頭へ戻してから表示することで、冒頭が一瞬見えて再スタートするように見える現象を抑制
- 表示Chrome検出後は実際のChromeウインドウPIDを保持し、停止時のウインドウ管理も安定化

## v1.0.29
- ITSUKI起動時にWindowsネイティブのスプラッシュ画面を追加。ITSUKIロゴ、正式名称、Version、更新日、起動ステータスを表示
- スプラッシュは最低1秒表示し、DB・動画情報・検索キャッシュ等の初期化に時間がかかる場合は実際の起動完了まで表示を継続
- 既存の ITSUKI.ico を実行時リソースにも同梱し、スプラッシュのロゴへ使用
- 管理画面上部のナビゲーションボタンを明るい文字・背景へ変更し、無効状態でも文字が読めるようコントラストを改善
- スタートは緑系、ストップは赤系、稼働状態バッジも明瞭な配色へ変更

## v1.0.28
- ITSUKI本体PCの通常リモコンで、数字を入力せず「メモリーセット」を4秒以内に10回押すと管理画面へ直接移動する隠しショートカットを追加
- この10連打ショートカットは接続元がITSUKI本体PCと確認できた場合だけ有効。スマホ・別PCでは発動しない
- 従来の「5963 → メモリーセット」経路もそのまま利用可能

## v1.0.27
- ITSUKIを起動しているPC自身からの管理画面アクセスは自動的に管理者扱いとし、管理者PIN入力を不要化
- 管理画面に「マスターURL」を追加。管理者スマホで1回開くと、そのブラウザを信頼済み管理端末として登録し、以後5963入力後のPINを省略可能
- マスターURLはLAN用 / Tailscale用を表示し、コピーとQRコードに対応
- 管理画面から登録済み管理端末の名前変更・個別解除・全解除が可能
- マスターURLを再発行して古い登録用URLを無効化可能。すでに登録済みの端末は維持
- 信頼済み端末トークンはブラウザCookieに保存し、ITSUKI側にはSHA-256ハッシュだけを保存
- 本体PCで管理者PINを初期値へリセットした場合は、登録済み管理端末も解除しマスターURLも再発行

## v1.0.26
- ITSUKI起動直後はTOP画面だけを開き、待機画面・予約一覧・VLCは管理画面の「スタート」を押すまで起動しない方式へ変更
- 管理画面上部に「スタート」「ストップ」を追加。スタートで待機画面と専用の予約一覧Chromeを起動し、ストップで待機画面・予約一覧・VLCだけを終了
- ストップしてもITSUKI本体・管理画面・予約データは維持するため、カラオケ中に設定変更へ戻って再スタート可能
- イベント中のタスクバー保護はITSUKI起動中ではなく「スタート」中だけ有効に変更し、ストップ時に元のWindows設定へ戻す
- DBや動画ルート等のファイル／フォルダ選択時に、待機画面・VLCを一時的に隠して復元する処理を廃止
- 停止中は予約追加やプレイヤー監視ループから待機画面・VLCが勝手に再起動しないよう制御を追加

## v1.0.25
- 履歴からの再予約で曲IDを基準に別動画が選ばれることがある問題を修正。履歴に保存された動画フルパスの完全一致だけで同じ動画を再選曲します
- 予約一覧の再生済み項目からの再予約も、予約時に保存した動画フルパスを基準に同じ動画を開くよう変更
- 履歴・ランキングで元動画が同じフルパスに存在しない場合は「動画なし / 予約不可」と表示し、別動画へ自動置換しません
- 予約画面で確定した動画フルパスを予約送信時にも再確認し、送信直前に動画が移動・削除されていた場合は予約を拒否します
- 履歴・予約一覧からの再予約は従来どおり予約送信画面を経由し、選曲者・一緒に歌う人・キー等を確認してから予約します

## v1.0.24
- 紐づけ監査Excelのインポートで、古い動画IDを先に参照したため別動画へ確認状態・曲IDが移る不具合を修正
- 監査Excelの対象動画は「DBセットID＋動画フルパス」の完全一致だけで特定し、動画ID・相対パス・ファイル名・パス正規化にはフォールバックしない安全仕様へ変更
- v1/v2監査Excelで内部技術列の行ずれを検出した場合、表示されている動画フルパスを基準に確認状態・確認メモ・曲IDを復旧可能に変更
- 今回の不具合で崩れた旧監査Excelは、v1.0.24で再取込するとフルパス基準で正しい動画へ復元可能
- 新しい監査Excel形式をv3へ更新。正常な新形式では従来どおり「修正後の曲ID」が空欄なら紐づけ変更なし

## v1.0.22
- Excelが日本語セルに保存するふりがな情報（rPh）を本文へ連結して読み込む不具合を修正。監査Excelの確認メモ／対応メモや履歴Excelでは、表示されている本文だけを取り込みます
- 監査Excelの条件付き書式用 `dxf` スタイルで XML 要素順が不正だった問題を修正。Excel が `styles.xml` を削除して全シートを修復する不具合を解消
- 監査Excel生成後の自己検証を強化し、styles.xml の要素順・宣言件数・セル style ID・条件付き書式 dxfId まで検査
- 監査Excelの条件付き書式の塗りつぶし指定をExcel互換の差分スタイルへ修正。確認済み=薄緑、要確認=薄赤、未対応通報あり=薄黄を背景色だけで強調し、文字色は通常表示を維持

- 紐づけ監査Excelに「現在のタグ」「修正後のタグ」を追加。DBシートのタグを参照し、同名・同歌手のLIVE / MV / PV等を判別しやすく改善
- 監査Excel形式をv2へ更新しつつ、v1.0.21以前のタグ列がない旧監査Excel（形式v1）も引き続きインポート可能
- 自動紐づけでも「確認済み」にした動画は曲IDを固定し、以後DBへ同名曲やより一致度の高い候補が追加されても再スキャンで変更しない仕様へ変更
- 自動紐づけのうち未確認の動画は従来どおり再スキャン時に現在のDBで再評価可能
- HELPの動画差し替え説明を整理。改名と同時に再エンコード・内容更新した動画は旧動画と安全に同一判定できないため、自動で紐づけ・確認状態を引き継がないことを明記

## v1.0.21

- 紐づけ監査の確認状態（未確認 / 確認済み / 要確認）を内部DBへ恒久保存し、再スキャン・監査Excel再出力後も維持
- 監査Excelへ条件付き書式を追加。確認済みは緑、要確認は行全体を赤、未対応通報ありは黄で強調
- 動画の削除・リネーム時に手動紐づけと監査状態を失いにくいよう、別テーブルへ紐づけ記憶を保存
- 純粋なリネーム/移動は、旧パスが消えていてファイルサイズ＋更新日時が一意に一致する場合、同一動画として手動紐づけ・確認状態を引き継ぐ
- 同じパスのファイル内容だけを置き換えた場合は既存の手動紐づけ・確認状態を維持。再エンコード＋改名で識別情報が変わった場合は安全のため自動引継ぎを保証しない
- HELPへ動画差し替え・改名・削除時の挙動を詳しく追記
- 手動で確定した動画↔曲IDの紐づけは再スキャンで自動上書きしないよう固定。DBへ同名曲やより一致度の高い曲を追加しても手動確定を優先
- 自動判定の動画は従来どおり再スキャンで再評価し、より適切な候補へ切り替え可能。自動紐づけ先が変わった場合は監査状態を未確認へ戻す
- 紐づけ記憶を改名先へ移行する処理を追加し、同じ動画を複数回リネームしても古い記憶が重複して復元不能になりにくいよう改善
- バックアップの「動画スキャン・曲紐づけDB」に、削除・改名動画用の紐づけ記憶も含めるよう拡張

## v1.0.20

- 待機画面の動画切り替えを改善。VLCの実際の再生終了を待ってから次へ進み、末尾を最大約1秒早く切り替える可能性があった終了判定を廃止
- 待機メディアはVLCプロセスの実終了を確認してから約0.01秒待って画面を切り替えるよう変更。動画の終端より先に次画面へ進まないように調整
- 次の待機動画は黒画面の裏でVLCを準備し、表示直前に先頭へ戻してから画面を切り替え、切替後に音量を戻す方式へ変更。pause / resume を使わず、先頭で停止したままになる問題を回避


## v1.0.19

- 管理画面「楽曲・紐づけ」に **紐づけ監査** を追加。未確認 / 要確認 / 通報あり / 未紐づけ / 手動 / 自動 / 確認済みで絞り込み、動画フルパスと現在の曲ID・曲名・歌手を一覧で確認可能
- 監査画面から1件ずつ「確認済み」「要確認」「曲を選び直す」「紐づけ解除」を実行可能
- **監査Excel（.xlsx）** の入出力に対応。大量の紐づけ確認・曲ID修正・確認状態変更・通報ステータス変更をExcel上でまとめて編集してITSUKIへ反映可能
- 複数DBモードでは1ブック内に `監査_01 / DB_01 / 通報_01` のようにDB＋動画セットごとのシートを自動生成
- 監査Excelでは現在値と修正後値を分離。修正後曲IDが空欄なら変更なし、`解除` なら紐づけ解除。DBに存在しない曲IDは取り込まずエラー表示
- 古い監査Excelを開いたままWeb側で紐づけや通報状態を変更した場合は競合を検出し、古い値で上書きしない安全策を追加
- 間違い通報に `未対応 / 確認中 / 対応済み / 誤報` のステータスと対応メモを追加。曲ID修正だけで自動解決にはせず、ステータス変更は明示操作のみ
- 楽曲DBそのものは監査Excelから書き換えません。対応曲がDBに無い場合はDB側へ追加・再読込後に監査Excelを再生成してください
- 監査Excel生成時、DB文字列・パス・通報内容にXMLで使用できない制御文字が含まれていても、Excelが破損しないよう自動除去するよう修正
- 監査ExcelのワークシートXMLで `autoFilter` と `mergeCells` の出力順がSpreadsheetML仕様と逆になっていた問題を修正。Excelがシートを修復・破棄する不具合を解消
- 監査Excel生成後にXML構造とワークシート要素順を内部検証し、不正なブックをそのままダウンロードさせないチェックを追加

## v1.0.18

- ジャンル別ランキングを最初に開いた際、先頭ジャンルが選択表示されているのに集計が未実行となる問題を修正
- 年別ランキングも初回表示時に、履歴が存在する最新年を選んで集計するように改善
- 公開README / Web HELPに、Xの `#ITSUKI_KaraokeApp` で更新・開発状況を確認できる案内を追加

## v1.0.17

- 履歴・ランキングの入出力をExcel（.xlsx）へ一本化。エクスポートと取込テンプレートを同一カラム構成に統一
- Excel先頭の「入力ガイド」シートに、基本入力・推奨・任意・通常入力不要の列を色分けして説明
- 曲ID・データ種別・動画パス等の技術列は通常入力不要としてグレー表示し、一部を非表示化。エクスポート時は自動保存
- 外部履歴では曲名・歌手・回数を中心に入力すれば、現在の楽曲DBと動画スキャン結果から可能な範囲で自動補完
- インポート後に動画を再スキャンした場合も、未補完の履歴へ動画情報を再照合

## v1.0.16

- 履歴・ランキングCSVの出力項目を拡張し、DB未紐づけ動画も欠落せず再インポートできるラウンドトリップ形式へ変更
- CSVに動画フルパス・相対パス・ファイル名・選曲者・同席歌唱者・キー変更などの識別情報を保存
- 再インポート後の動画照合を「フルパス → 相対パス → 一意なファイル名」の順で行い、動画が見つからない場合も履歴自体は保持
- 従来の簡易CSVテンプレートもそのままインポート可能

## v1.0.15

- ITSUKI導入前のExcel / Googleスプレッドシート等の履歴をCSVで取り込み、既存履歴と合わせてランキング集計できる機能を追加。CSV出力と、楽曲DBを検索して曲を選ぶ1件ずつの追加にも対応
- ランキングページに「総合 / 年別 / ジャンル別」タブを追加し、表示件数を管理画面から10～500曲で設定可能に変更
- 楽曲DBへ紐づいた曲について、歌手ごとの上位3曲を内部集計し、曲詳細に「歌手別1位」などのタグを表示
- 実再生履歴が一度もないDB紐づけ曲へ「再生ゼロ曲」タグを表示。DB未紐づけ動画は再生ゼロ判定の対象外
- TOPに「再生ゼロ曲」を追加し、同じ歌手・連名・直接の別名義 / グループが偏りにくいランダムおすすめを表示
- TOPの中サイズボタンを管理画面から個別に表示 / 非表示できるように変更。大ボタンと下段のリモコン系小ボタンは固定表示

## v1.0.14

- 待機画面プレイリストにファイルだけでなくフォルダも登録できるように変更。フォルダ配下（サブフォルダ含む）の画像・動画・音声を自動展開
- 待機メディアの再生順に「順番再生 / ランダム再生」を追加
- ランダム再生は全対象を1巡するまで同じ項目を重複させないシャッフル方式を採用し、周回境界でも可能な限り同じ項目の連続再生を回避
- フォルダ展開結果を30秒キャッシュし、待機監視ループが毎回ディスクを走査しないように最適化

## v1.0.13

- 人物ページで関連人物を辿った際、検索結果へ戻るボタンとは別に「← 1つ前の人物名 に戻る」ボタンを表示
- 関連人物を複数段辿っても、人物ページ内の履歴を1段ずつ戻れるように改善

## v1.0.12

- 人物検索で、複数人歌唱の連名クレジットを従来どおり1つの歌手候補として検索できるように修正
- 連名に含まれる歌手名から連名候補を検索でき、連名ページではその完全な歌手クレジットの曲だけを表示
- 人物の別名義・所属グループ・所属メンバーなどの関連展開をDB上の直接関係（1段階）だけに制限し、関連の連鎖による候補増殖を防止

## v1.0.11

- TOPの「歌手名」を「人物検索」へ変更し、歌手・作詞・作曲・編曲に登場する人物をまとめて検索できるように変更
- 人物を選択すると検索タブが「歌手 / 作詞 / 作曲 / 編曲」に切り替わり、担当ごとの曲一覧を横断表示
- 該当曲が0件の担当タブはグレーアウトして選択不可にし、担当ごとのヒット数も表示
- 人物リレーションに別名義・所属グループ・所属メンバーがある場合、「別名義・グループ」から関連人物へ移動できる機能を追加

## v1.0.10

- 管理画面の「一般」「連携・バックアップ」「楽曲・紐づけ」「システム」もDB・再生と同じ2列の設定レイアウトへ統一
- 長い説明は「詳しい説明」「この機能について」から必要なときだけ展開する方式へ整理
- 入力欄・スイッチ・操作ボタンの配置を揃え、設定項目を縦に追いやすく改善
- システムページの更新情報や操作部品が横方向へはみ出す問題を修正

## v1.0.9

- 管理画面「DB・再生」をChrome設定画面に近い2列レイアウトへ整理し、設定名と操作欄の位置を統一
- 長い補足説明を「詳しい説明」で必要なときだけ展開する方式へ変更
- DB / スキャン対象 / VLC・再生 / 待機画面 / 音量・ファイル名判定のグループに整理
- ON/OFF設定をコンパクトなスイッチ表示へ変更し、設定保存・再スキャン操作を画面下部にまとめて表示

## v1.0.8

- 「演奏停止」受信時、フェード中だけVLC画面上部に赤文字で「演奏を停止します」と表示するように変更
- 手動停止フェードをVLCの0～256生音量値で1段階ずつ下げる方式に変更し、従来より細かく滑らかに調整
- 管理画面に「VLCキー変更機能」を追加。OFF時は選曲画面・リモコンからキー関連UIを完全に非表示
- キー変更ON時、選曲予約画面に -8～+8 の数直線型キーセレクターを追加し、予約ごとのキーを保存
- 再生中のリモコンに「キー− / ±0 / キー＋」を追加し、半音ずつリアルタイム変更。変更時はVLC画面上部に現在値を表示

## v1.0.7

- リモコンの「演奏停止」時に、動画を再生したまま音量を滑らかに0までフェードアウトしてから一時停止するように変更
- フェードアウト完了後にNEXT / 目安Kcalを表示し、既存の「曲終了後のNEXT表示時間」経過後に予約一覧または待機画面へ進むように変更
- 管理画面に「演奏停止時のフェードアウト時間（秒）」を追加。0～5秒で調整可能、標準1秒。自然終了時の挙動には影響しません

## v1.0.6

- DBごとに「タイアップなしとして扱う値」を設定できる機能を追加。標準は `-` で、`なし` / `N/A` など複数の表記を指定可能
- タイアップなし判定に一致した値は、検索結果・タイアップ一覧などで空欄として扱うように変更
- 管理画面を「一般」「DB・再生」「連携・バックアップ」「楽曲・紐づけ」「システム」の5ページ切替に変更し、縦長表示を改善

## v1.0.5

- リモコンから管理者認証へ入る操作 `5963` →「メモリーセット」→ 管理者PIN →「メモリーセット」をHELPへ明記
- ITSUKI本体PCのタスクトレイから管理者PINを初期値 `0000` へ戻せる復旧機能を追加
- PINリセット時は既存の管理者ログインセッションを解除し、スマホ・別PCからはリセットできない本体PC限定方式に変更

## v1.0.4

- 管理画面からTOP画面へ任意の外部リンクボタンを追加できる機能を追加
- 追加ボタンは表示名・絵文字・色・URL・表示ON/OFFを設定でき、並び替え・削除に対応
- 追加ボタンはTOPの中ボタンと小ボタンの間へ表示し、リンクは新しいタブで開く
- 追加ボタン数は上限なし。設定はPC固有のconfig.jsonへ保存し、配布版には含めない

## v1.0.3

- HELPの楽曲DB説明に、外国語曲 / パート分け / アニメ・ゲーム映像 / MV・PV / LIVE の分類条件を追加

## v1.0.2

- 管理者HELPの便利機能にTailscaleで外部ネットワークからアクセスする手順を追加
- Tailscale公式ダウンロードページへのリンクを追加
- ITSUKIのリモコンにあるLAN / Tailscale QR切替の使い方を追記

## v1.0.0

- ITSUKI 初回公開版
- 曲終了 / 演奏停止時のNEXT表示と目安Kcal演出を正式搭載
- Kcalは `00.0` 形式の3桁7セグ表示、最大99.9 Kcalに調整
- 新曲・更新曲の週別表示、歌手検索フィルター、一緒に歌う人、間違い通報、とりあえず300などを搭載
- ローカル同梱HELPとGitHub Pages向けHELPを同一原本から生成
- 更新確認は公式GitHub Releaseを参照し、初回Release公開前の404を正常な「Release未公開」状態として扱う
- 管理画面の待機BGM標準音量変更を待機再生へ即時反映

## v0.6.4

- 選曲予約画面のフルパスは、必要な時だけ `\` / `/` を折り返し候補にして横幅に合わせて改行
- TOPの「新曲・更新曲」表記を復元
- 新曲・更新曲の週ナビを4週固定から、管理画面の表示日数に含まれる全WEEKの可変表示へ変更
- WEEKが横幅に収まらない場合は横スクロールし、選択中の週を中央寄せ＋両端フェードで前後週を視認可能に変更
- 曲終了時のNEXT帯を参考デザイン寄りの斜めピンクタブ＋明色レールへ調整
- Kcalメーターを多重リング化し、数値を独自7セグ描画に変更

## v0.6.3

- 曲終了時のNEXT表示を見やすく調整（NEXT文字・帯を大型化）
- Kcal表示を参考デザイン寄りに調整し、数値を円内で大きく表示
- 新曲・更新曲を1週間ずつ表示するページ式へ変更
- 新曲ページ上部に `◁ [Wxx] Wxx ... ▷` の週移動ナビを追加

## v0.6.2

- 曲終了時のNEXTオーバーレイを、予約通知と同じ64bit対応Win32描画基盤へ統一
- 全画面の透明レイヤー方式をやめ、NEXT帯とKcal表示部分だけをTOPMOST表示する方式へ変更
- NEXTウインドウ生成時の64bitハンドル破損を修正


## v0.6.1

- 曲終了時のNEXT表示と次曲遷移を安定化
- 「演奏停止」でも現在フレームを止めてNEXT/Kcalを表示
- 新曲・更新曲一覧を起動時・再スキャン時に事前生成して高速化
- 「一緒に歌う人」を折りたたみ式に変更
- 選曲画面の動画フルパス表示を見やすく調整
- `stop.bat` のlocalhost終了処理を修正
